{
  "name": "f2nd-html-build",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && npx @tailwindcss/cli@4 -i input.css -o dist/tailwind.css && cp -r assets index.html script.js style.css dist/"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}
